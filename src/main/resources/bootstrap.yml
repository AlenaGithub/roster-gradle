---
management:
  security:
    enabled: false

security:
  basic:
    enabled: false
 
---
spring:
  application:
    name: roster
  cloud:
    config:
      uri: https://alena:alena123@configserver-inarticulate-executiveness.cfapps.io 
      #${vcap.services.configserver-service.credentials.url}